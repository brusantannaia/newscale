[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "18"

# Redireciona /admin para /admin/ com barra no final
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
